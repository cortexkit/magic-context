---
task_id: bg_49897ae8
agent: Alfonso-Junior
session_id: ses_213055928ffeSxZdyXKHCVv5wz
parent_session_id: ses_331acff95fferWZOYF1pG0cjOn
status: running
completed_at: unknown
---

[user] 2026-05-03T08:35:42.939Z
You are auditing `packages/pi-plugin/` in the repo at `/Users/ufukaltinok/Work/OSS/opencode-magic-context` for its **first public release** readiness.

The Pi plugin (`@cortexkit/pi-magic-context`) is a new package that extends Pi coding agent with Magic Context features (historian, memory, dreamer, etc.) using a shared SQLite database with the OpenCode plugin.

Your job: thorough audit of the entire `packages/pi-plugin/src/` codebase for:

**Release blockers:**
- Crashes, panics, incorrect error handling that would break a fresh install
- Missing cleanup paths (temp files, DB handles, child processes)
- Hardcoded dev paths or debug artifacts that shouldn't ship
- Package.json `files`, `main`, `bin` entries — are all referenced files actually built and present?
- Any `packages/pi-plugin/dist/` artifacts that need to exist at publish time

**Correctness concerns:**
- Core features: historian triggering, compartment injection, tool registration, system prompt injection
- Cross-harness memory sharing (writes under correct project identity?)
- Session cleanup (are session-scoped maps cleaned on session end?)
- Config loading: does it fall back correctly when no config file exists?
- Subagent runner: does `--no-session` work correctly? Does it avoid polluting Pi's session list?
- CLI: `setup` and `doctor` commands — do they work standalone under Node?

**Documentation & packaging:**
- Is there a README in packages/pi-plugin/?
- Does `package.json` have correct `description`, `keywords`, `repository`, `license`?
- Is `CHANGELOG` or release notes needed?
- Are `peerDependencies` correct for `@mariozechner/pi-coding-agent`?

**Test coverage gaps:**
- Which critical paths have no test coverage?
- Are e2e tests adequate for the core scenarios?

Read the actual source files. Don't speculate. Return a structured report:
- RELEASE_BLOCKERS (must fix before shipping)
- HIGH_PRIORITY (should fix before or with first release)
- LOW_PRIORITY (can defer)
- PACKAGING_CHECKLIST (what needs to be done before `npm publish`)
- CONFIRMED_CLEAN (things explicitly verified as fine)
<!-- ALFONSO_INTERNAL_INITIATOR -->
