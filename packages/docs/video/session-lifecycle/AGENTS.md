# Editing this video

Read `BRIEF.md`, `storyboard.md`, and `README.md` before changing the story. Read the installed `/hyperframes` and `/hyperframes-core` skills before changing the composition contract.

Keep the same context-window bar visible for all 84 seconds. Use the sanctioned vocabulary and preserve the distinction between queued drops, historian compartments, and cached prompt history. Check configuration claims against the repository source, not the illustrative segment sizes.

Use one seekable, paused GSAP timeline registered as `session`. Do not use clocks, random values, animation timers, or render-time remote assets. Keep timed clips' visibility under HyperFrames control. Update `storyboard.md` whenever on-screen copy changes.

Run `npm run check`, render with `npm run render`, and inspect frames from the exported MP4. Rendering is local only. Do not publish, deploy, commit rendered artifacts, or add unrelated workspace dependencies.
